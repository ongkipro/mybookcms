# Audit — 2026-08-23

> **Upstream lineage, not MyBookCMS.** This audited a fleet of other installs
> before the Malaysia cutover. Sibling install identities, hostnames and
> Cloudflare identifiers are redacted: they map someone's other businesses and
> mean nothing here. Findings below are a point in time on a different runtime.
>
> Archived upstream audit. It predates the MyBookCMS Malaysia cutover and is
> retained only as historical evidence. Provider, advertising, tenant, route,
> test-count, and live-install statements below do not describe the current
> MyBookCMS runtime. Use root `STATUS.md`, `ARCHITECTURE.md`, `PRD.md`, and
> `TASKS.md` for current truth.

> Verified against disk: 2026-08-23 @ `c354143`

A point-in-time report, not a source of truth. Structure is owned by
`ARCHITECTURE.md`, implemented behaviour by `STATUS.md`, remaining work by
`TASKS.md`, history by `BUILD-LOG.md`. Where a
finding belongs to one of those, it says so and the fix goes there — nothing
here is meant to be maintained.

Method: the product tree at `c354143`, plus five live installs measured from
outside. Every number below was produced by running something. Where a claim
could not be verified it is marked as unverified rather than softened.

---

## 1. What the audit found healthy

Recorded because an audit that only lists problems misrepresents the system.

**Admin authorisation is centrally enforced and has already survived its worst
bug.** `middleware.ts` classifies private paths from `context.url` — the
pathname Astro actually routed on — after an incident where `//api/admin/settings`
and `/%61pi/admin/settings` classified as public while Astro served them as
admin. Unsafe methods on `/api/admin/*` additionally require a same-origin
`Origin` and reject `sec-fetch-site: cross-site`. No admin route re-implements
this, which is why grepping the route files for an auth call finds nothing —
the check is one layer up, where it cannot be forgotten.

**The headless surface is not an unguarded copy of the admin surface.**
`/api/v1/*` routes go through `validateHeadlessRequest()`: a developer API key,
an origin allowlist resolved per store, and an audit row. The OpenAPI document
is the only public member.

**The retired payment webhook is a tombstone, not a deletion.**
`/api/webhooks/autolaris` answers `410` with an explicit code. A deleted route
would 404 and invite a retry to be re-pointed at something new; this one cannot
mutate payment state and says why.

**Discipline markers are real.** One `TODO`-class comment in 59k lines of
source, and it is a `// lazy:` naming its own ceiling
(`notifications.ts`: no retention job, one row per order forever). 504 tests
pass, `astro check && tsc --noEmit` reports 0 errors across 375 files, and the
dependency list carries nothing that a platform feature already does.

**Five installs run the current product.** `install-1.example`, `install-2.example`,
`install-3.example`, `install-4.example` and `install-6.example` all serve Astro 7.2 and
all answer `401` on `/api/admin/health` — the admin boundary holds on every one.

---

## 2. Findings

### 2.1 `STATUS.md` names one install, and it does not resolve

`STATUS.md` § "Known installs" lists `install-7.example` as the only install.
That hostname has no DNS record at all:

```
$ dig +short install-7.example
(empty)
$ curl https://install-7.example/
curl: (6) Could not resolve host
```

Meanwhile the five installs in §1 are unlisted. The document that tells a
future reader where this product actually runs is pointing at the one place it
does not.

This matters beyond tidiness: `STATUS.md` is what decides where a fix has to be
adopted. **Owner: `STATUS.md`.** Fixed in this branch.

### 2.2 The CAPI outbox has no clock of its own

`drainCapiOutbox()` is called from exactly two places, both request handlers:
`/api/meta-event` and `/api/v1/tracking/events`. The hourly cron in
`worker.ts` runs `ensureSchemaUpgraded`, `purgeExpiredAbandonedOrders`,
`collectOperationalHealth` and `evaluateOperationalAlerts` — and does not drain
the outbox.

So a delivery failure is retried only when the next visitor arrives. Backoff
caps at `CAPI_MAX_BACKOFF_MS` (60 minutes), which assumes something will be
along within the hour to act on it. On a store between campaigns that is not
true, and a Purchase that failed at 23:50 waits for traffic rather than for
time.

The system already *detects* this — `collectOperationalHealth` counts pending
rows whose `next_retry_at` has passed — so the cron computes the number and
then does not act on it. The binding, the schedule and the query all exist;
what is missing is three lines wiring them together. The store's pixel ID and
CAPI token are a D1 read the cron is already positioned to make.

Not observed failing: `install-1.example` shows 3115 of 3115 events `sent`.
This is a liveness gap, not an outage.

**Owner: a product change.** Not fixed here — it needs a decision about how
the scheduled path resolves store ad config, which is the same question a
multi-store cron would ask.

### 2.3 An auto-rotating hero is structurally incompatible with Speed Index

Reported in #53 and partly addressed in #54: `startHero()` used to run at parse
time, so the first advance landed 4 s in and painted a fresh LCP candidate.
Moving the start to `load` helped LCP but did not fix the page, and the
follow-up measurement says so plainly. Home page, three runs after the fix
shipped:

| | LCP | Speed Index |
| --- | --- | --- |
| median of 3 | 3.2 s | 6.0 s |

The LCP element is still a slide at `left: -409px` — the track has advanced
before the trace ends — and Speed Index is unchanged, because it integrates
visual completeness over the whole trace and a page that keeps changing never
reaches it. Deferring the start moves the cost; it does not remove it.

**Resolved in #59 by taking option (1): the timer is gone.** The dots stay, so
every slide is one tap away on a 44 px target. Measured on the live store,
three runs each side:

| | perf median (range) | LCP | Speed Index | TBT |
| --- | --- | --- | --- | --- |
| auto-rotating | 79 (77–85) | 3.2 s | **6.0 s** | 362 ms |
| after | **86 (83–93)** | **1.8 s** | **1.9 s** | 326 ms |

Speed Index is the number that settles it. Deferring the start (#54) had left
it at 6.0 s, unchanged; removing the rotation took it to 1.9 s. The run-to-run
range also tightened, which is the same finding from the other direction — the
spread was the carousel, not the network.

### 2.4 Third-party tags dominate blocking time, and deferral cannot fix that

Product page, median of four runs after #54 deferred `gtag.js`:

```
Facebook            530 ms blocking   226 KB
Google Tag Manager  218 ms blocking   153 KB
everything else       0 ms
```

Total Blocking Time moved 740 ms → 778 ms median, i.e. not at all. This was
predictable in hindsight and is worth writing down so it is not re-attempted:
deferring a script by 2500 ms moves *when* it parses, and Lighthouse's trace
runs long enough to include it either way. Deferral is still right — it takes
the download off the critical path, and FCP and LCP both improved — but it buys
nothing on TBT.

TBT on these pages is therefore a floor set by Meta and Google, not a defect.
The only lever that would move it is sending fewer tags, which is the opposite
of what the storefront is for. **No action. Recorded so the next audit does not
open the same ticket.**

### 2.5 `label-content-name-mismatch` needed two attempts

Worth recording because the first fix was wrong in an instructive way. The
brand link failed WCAG 2.5.3 because its visible text was not contained in its
accessible name. The first attempt (#54) added the missing space between
`{leadWord}` and `{trailingWords}` — a real defect, but not this one. The audit
kept failing.

The actual cause was the monogram: a decorative `Z` rendered as a text node
beside a wordmark that already spells the name. `aria-hidden` keeps it out of
the accessible name but not out of what the audit reads as visible text.
Drawing it from `data-monogram` via CSS `content` removes the text node
entirely (#55). Fixed.

### 2.6 Test coverage has a shape worth knowing

84 test files against 154 non-test modules in `src/lib`. Eighteen modules have
no direct test; ranked by size:

```
province.ts 191   tenant-content.ts 111   catalog.ts 97   autolaris-balance.ts 95
ui-variants.ts 91  store-ads.ts 81        geo.ts 77       env.ts 75
rts-scoring.ts 65  gtm.ts 54              (8 more under 31 lines)
```

Most are thin or exercised through their callers. Two are worth a direct test
on their own merits rather than for the count:

- **`store-ads.ts`** resolves the pixel ID, CAPI token and Google Ads
  identifiers every tracking path depends on. A silent misresolution here
  disables tracking store-wide and nothing fails loudly.
- **`rts-scoring.ts`** scores return-to-sender risk, which is money.

`province.ts` at 191 lines is almost certainly a data table and does not need
one. **Owner: `TASKS.md`** if adopted.

### 2.7 `/landing-page` — #45 item 1 is stale, item 2 is not

The index route now exists and serves:

```
install-1.example/landing-page    200
install-2.example/landing-page       404
install-3.example/landing-page  404
```

The two 404s are stores with no landing pages, so the honest question is
whether a store with an empty list should 404 or serve an empty state. The
home page links to it unconditionally, so today a fresh install ships a link to
a 404. **Owner: #45**, which should be re-scoped to that rather than closed.

Item 2 of #45 — `brand-contamination` failing in any install whose catalogue
images live in `public/` — is unchanged and still fails on `install-1.example`
(503/504 pass there against 504/504 here). The test scans source for
references; the references are in D1. Until it can read the database, or is
scoped to the product's own assets, every install runs with one permanently
red test, which is worse than no test: a real contamination would not be
noticed.

---

### 2.8 Where the `srcset` item of #53 actually belongs

Recorded because the obvious fix would have been the wrong one.

`src/lib/image-derivative.ts` already does this: the card renders at 182 CSS px
against a 480 CSS px detail view, so `cardImageSrc()` points the tile at a
`<name>-sm.<ext>` derivative that `upload-r2.ts` writes at upload time, and
`/assets/[...key].ts` falls back to the original when the derivative is absent.

What Lighthouse flags are static `public/images/*.webp` files, which never pass
through the uploader and so never get one. Extending `cardImageSrc()` to
rewrite `/images/` paths would be worse than the problem: those are served by
the `ASSETS` binding, which has no fallback, so a missing `-sm` would 404 the
tile instead of costing bytes.

**Not a product gap.** The install either puts catalogue images through the
admin uploader or ships them right-sized.

---

## 3. What was fixed during this audit

| Finding | Where | Ref |
| --- | --- | --- |
| 96% of CAPI events carried no `user_data`; no event ever carried `fbc` | product | #54 |
| Hero rotation started at parse time | product | #54 |
| `gtag.js` loaded on the critical path | product | #54 |
| Brand monogram counted as link text | product | #55 |
| `ink-muted` failed AA on `primary-soft` | `install-1` install | store commit |
| `public/robots.txt` shadowed the dynamic route | `install-1` install | store commit |
| 24 images at 5.4 MB where 1.8 MB was enough | `install-1` install | store commit |
| CAPI outbox retried only on traffic, never on time | product | #59 |
| Hero auto-rotation owned LCP and Speed Index | product | #59 |
| `brand-contamination` permanently red in every install | product | #59 |

The tracking fix was verified against production rather than asserted: a
`ViewContent` posted with an empty body, after a request carrying `?fbclid=`,
came back `{"success":true,"delivered":true}` with
`"fbc":"fb.1.1787426645087.IwAR_verify_26630"` recorded on the outbox row.

---

## 4. Not findings

Checked and deliberately left alone.

- **`best-practices: 79` on every storefront page.** `third-party-cookies` plus
  the cookie entries under `inspector-issues`, all Meta and Google. It is the
  cost of the tracking the product exists to deliver.
- **`seo: 92`.** Cloudflare's managed robots.txt injects
  `Content-Signal: search=yes,ai-train=no,use=reference`, which Lighthouse does
  not recognise as a directive. Not the product's file; disabling it is a
  per-zone dashboard decision.
- **`notifications` growing one row per order.** Already marked `// lazy:` with
  its ceiling stated. Correct as a recorded trade, not a defect.
- **48 migrations with no registry.** `bundled-migrations.ts` expands a Vite
  glob at build time, so the chain cannot drift from the directory.

---

## 5. Addendum — what the fleet rollout surfaced

Written after rolling `fb1218d` to four of the five installs the same day.

### 5.1 Two installs exist on one disk only

`install-2` and `install-3` have a `product` remote and no `origin`. Their
install commits — the Cloudflare resource IDs, the domain wiring, the
deliberate cron release — exist nowhere but this machine. A deploy can be
re-run from a lost tree only by re-deriving those IDs from the Cloudflare
dashboard by hand.

`install-1` and `install-4` both push to GitHub. The other two should.
**Owner: an operator action, not a product change.**

### 5.2 An install decision survives a merge only by luck of the diff

`install-3` released its cron slot by *commenting out* the `triggers`
block rather than deleting it. That is what let a 31-commit merge from the
product leave the release intact — git saw a modified line, not a removed
block, and the product had not touched it.

`install-1` is protected structurally instead: its sync script lists
`wrangler.jsonc` among the files a sync must never overwrite. The four
merge-based installs have no equivalent, so every install decision that lives
in `wrangler.jsonc` depends on the product not touching the same lines. It
held this time. It is not a guarantee.

### 5.3 Three installs are ready for tracking and have none configured

`install-2`, `install-3` and `install-4` all have empty `meta_pixel_id`,
`meta_capi_token`, `google_ads_conversion_id` and
`google_ads_conversion_label`. Their tracking components correctly render
nothing, so the pages are clean rather than broken — but nothing is being
measured on any of them.

One half already works regardless of that config: `_fbc` is captured by the
middleware from `?fbclid=`, verified live on all four hosts, so ad clicks are
being recorded on the order even where no pixel is installed. Entering the
IDs in `/admin/settings` is all that is outstanding, and it is an operator
action.

---

## 6. Addendum — local multi-worktree and D1 audit

Verified later on 2026-08-23 against every local worktree. This addendum records
the pre-remediation state requested for the audit; it does not change product
code, migrations, local data, branches, or remote resources.

### 6.1 Worktree and executable-evidence matrix

| Worktree | Revision/state | Tests | Check | Build | Local D1 |
| --- | --- | ---: | --- | --- | --- |
| `install-11` | `96a2cee`, `main`, one commit behind `origin/main`; one pre-existing modified storefront file | 504/504 | pass | pass | 48/48 migrations; foreign-key check clean |
| `adsbookcms-dev` | `7883095`, detached HEAD, clean | 431/431 | pass | pass | 45/45 migrations; foreign-key check clean |
| `adsbookcms-lp` | `fe59ab1`, `fix/audit-followups`, clean; upstream branch gone | 504/504 | pass | pass | 48/48 migrations; foreign-key check clean |

For each worktree, the complete migration chain also applied to a fresh,
isolated local D1 database. `npm audit --omit=dev` reported zero known
production dependency vulnerabilities for the current and older lockfile
states. A source scan found no recognized hard-coded secret literals.

`PRAGMA integrity_check` cannot be used through the local D1 emulator because
the command is rejected with `SQLITE_AUTH`. That is an audit limitation, not
evidence of corruption. The supported `PRAGMA foreign_key_check` completed
with no rows on both the existing and freshly migrated local databases. No
remote D1 database or production installation was queried.

### 6.2 HIGH — stored script injection through form JSON configuration

Both canonical storefront forms render DB-backed product data into an inline
JSON script using raw `set:html={JSON.stringify(...)}`:

- `src/components/storefront/forms/FormHybridContent.astro:253`
- `src/components/storefront/forms/FormMiddleContent.astro:211`

`JSON.stringify()` escapes JSON syntax but does not escape `<`. Consequently a
product name such as:

```html
</script><script>globalThis.__xss=1</script>
```

terminates the `application/json` element at the HTML-parser layer and creates
an executable script element. The script MIME type does not protect the page
after the original element has been closed. The same sink exists at all three
local revisions and at current `origin/main`.

Impact: catalog content with a hostile product name can execute JavaScript in
the storefront origin, allowing interception of buyer form input and actions.
The immediate write path is operator-controlled, so this is not an anonymous
write primitive, but persisted or imported catalog data must still be treated
as untrusted at an HTML raw-text boundary.

The repository already contains the correct pattern elsewhere:
`src/components/storefront/seo/JsonLdSchema.astro:58` and
`src/pages/produk/[slug].astro:125` escape `<` as `\u003c` before using
`set:html`. The form configuration needs the same invariant plus a regression
test containing a literal `</script>` payload.

**Owner: product security fix and regression test; record the accepted work in
`TASKS.md` before implementation. Not fixed during this audit.**

### 6.3 MEDIUM — local worktrees execute materially different product states

The directory most likely to be treated as canonical, `install-11`, is one
commit behind `origin/main`. That missing remote commit contains the scheduled
CAPI outbox drain described in finding 2.2, removes automatic hero movement,
and corrects the install-only asset test. `adsbookcms-lp` contains the same
product fix under local commit `fe59ab1`, while its tracked remote branch has
already been deleted. `adsbookcms-dev` is a detached 1.2.0-era revision with
three fewer migrations and 73 fewer tests than the two 1.3.0 worktrees.

This is not a failing build, but it is an operational ambiguity: terminals
opened in similarly named directories run different schema and runtime
behaviour, and work started in `adsbookcms-dev` is not attached to a branch.
It directly permits the cross-terminal overlap and contradictory conclusions
that a single canonical working directory is meant to prevent.

**Owner: operator worktree reconciliation. Preserve the pre-existing change in
`src/components/storefront/ProductImageGallery.astro`; do not delete or reset a
worktree until its purpose and any uncommitted work have been confirmed. Not
changed during this audit.**

### 6.4 Findings disposition

- One active product-security defect is proven across every audited revision:
  the form JSON script breakout in 6.2.
- One local development-state gap is proven: worktree/branch/schema drift in
  6.3.
- No migration ordering error, migration application failure, foreign-key
  violation, type error, build error, unit-test failure, known production
  dependency advisory, or hard-coded secret was found.
- Candidate raw-HTML sinks in JSON-LD and the landing-page editor were reviewed
  and are not findings: JSON-LD escapes the raw-text terminator, while the
  editor preview removes forbidden elements, event attributes, and non-HTTP(S)
  URLs before rendering.
## 7. Addendum — the product took a house accent while this audit was open

`#58` landed a storefront refresh that introduced five tokens into
`src/styles/form-hybrid.css`:

```css
--sf-accent: #2c6ecb;  --sf-accent-strong: #1f5199;  --sf-accent-soft: #eef4fc;
--sf-field-border: #c9cccf;  --sf-surface-alt: #f6f8fb;
```

ADR-019 (Accepted) says the public surface is monochrome, on the grounds that
*"this repository is not a brand. It is a CMS that installs for any merchant."*
That argument applies here unchanged. What does **not** carry over is ADR-019's
other half: the gold it removed was 2.26:1 on white and failing AA as text in
ten places. This set passes — `#2c6ecb` is 5.00:1 on white and 5.00:1 under
white. So this is a branding question, not an accessibility one.

The part that is better than last time: five custom properties instead of 48
hardcoded usages, so an install re-brands by overriding declarations rather
than patching call sites. That suggests amending ADR-019 — *one accent,
expressed as overridable tokens, never hardcoded at a call site* — rather than
reverting #58. Raised as **#60**; the code and the ADR currently disagree and
one of them has to move.

Cost already paid downstream: `install-1` maps the product's neutrals onto its
own deep blue with a store-owned script, and had to add seven mappings or ship
Shopify blue on its checkout form. Every future install pays the same tax, and
the mapping is discoverable only by diffing the product's CSS after a release.
Documenting the five tokens as *the* re-brand surface would make that a
contract instead of archaeology.
