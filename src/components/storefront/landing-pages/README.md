# Native Astro landing page components

This directory is the public-only component boundary for future hand-authored
landing pages. Keep static sections as `.astro` components and add a hydrated
island only for real interaction. Shared storefront chrome stays in sibling
directories (`shared/`, `tracking/`, and `seo/`); do not import CMS components.
