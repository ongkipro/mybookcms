---
import { cn } from '../../../lib/cn';

interface BreadcrumbItem {
  label: string;
  href?: string;
}

interface Props {
  items: BreadcrumbItem[];
}

const { items } = Astro.props;
const rail = 'max-w-[480px]';
---

<nav aria-label="Breadcrumb" class="border-b border-[#E5E5E5] bg-white px-4 py-2">
  <div
    class={cn(
      'mx-auto flex min-w-0 items-center gap-1.5 overflow-hidden text-[11px] text-[#555555]',
      rail,
    )}
  >
    {
      items.map((item, index) => {
        const isLast = index === items.length - 1;

        return (
          <>
            {index > 0 && <span class="shrink-0 text-[#767676]">/</span>}
            {item.href ? (
              <a href={item.href} class="shrink-0 transition-colors hover:text-[#111111]">
                {item.label}
              </a>
            ) : (
              <span
                class="min-w-0 max-w-[58%] truncate font-normal text-[#111111] sm:max-w-[65%]"
                aria-current={isLast ? 'page' : undefined}
              >
                {item.label}
              </span>
            )}
          </>
        );
      })
    }
  </div>
</nav>
