---
export const prerender = false;
// Retired Indonesian slug. The canonical public address is Malay (REQ-203);
// this keeps indexed and shared `/kebijakan-privasi` links resolving with one hop.
return Astro.redirect(`/dasar-privasi${Astro.url.search}`, 308);
---
