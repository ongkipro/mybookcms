---
export const prerender = false;
// Retired Indonesian slug. The canonical public address is Malay (REQ-203);
// this keeps indexed and shared `/kontak` links resolving with one hop.
return Astro.redirect(`/hubungi-kami${Astro.url.search}`, 308);
---
