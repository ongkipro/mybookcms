---
export const prerender = false;
// Retired English slug. The canonical public address is Malay (REQ-203).
// `/api/landing-page` and any `/admin/...` route are contracts, not public copy,
// and are deliberately untouched.
return Astro.redirect(`/halaman${Astro.url.search}`, 308);
---
