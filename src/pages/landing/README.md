# Native Astro landing routes

Place a public, hand-authored landing page here as `<slug>.astro`.

Each route must use `BaseLayout`, render useful HTML before enhancement, and
reuse components from `src/components/storefront/landing-pages/`. Do not put
CMS-generated landing-page routes here: those remain owned by `/admin/landing-pages`
and their existing dynamic storefront route.
