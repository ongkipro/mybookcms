---
export const prerender = false;
// Was a 301 to /landing-page back when that had no route and 404'd. The page
// index now lives at /halaman, but this legacy address predates it and its
// inbound traffic wanted the catalog, so it keeps pointing there.
return Astro.redirect('/produk', 301);
---
