---
export const prerender = false;
// Retired Indonesian slug. The canonical public address is Malay (REQ-203);
// this keeps indexed and shared `/syarat-ketentuan` links resolving with one hop.
return Astro.redirect(`/terma-syarat${Astro.url.search}`, 308);
---
